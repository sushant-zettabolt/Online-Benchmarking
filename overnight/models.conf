# Editable model list. Whitespace-separated, '#' comments allowed.
# columns: name  backend   quant  src_path (NFS source, staged to /tmp/models/<basename> on each inference pod)  served_name
#
# 'quant' values used by matrix.conf: bf16, q8 (llama.cpp Q8_0 GGUF), w8a8 (vLLM INT8 W8A8).
# served_name is only meaningful for vllm rows (the --served-model-name); use '-' for llamacpp rows.
#
# To add a new model: add 4 rows here (llamacpp bf16, llamacpp q8-or-similar, vllm bf16, vllm quant),
# then reference its `name` in matrix.conf. No script changes needed.

llama31-8b  llamacpp  bf16  /proj/rdi/staff/sacsharm/models/gguf/Llama-3.1-8B-Instruct-BF16.gguf              -
llama31-8b  llamacpp  q8    /proj/rdi/staff/sacsharm/models/gguf/Llama-3.1-8B-Instruct-Q8_0.gguf               -
llama31-8b  vllm      bf16  /proj/rdi/staff/sacsharm/models/hf/Llama-3.1-8B-Instruct                           llama31-8b
llama31-8b  vllm      w8a8  /proj/rdi/staff/sacsharm/models/hf/Meta-Llama-3.1-8B-Instruct-quantized.w8a8       llama31-8b-w8a8
